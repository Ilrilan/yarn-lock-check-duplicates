{
  "name": "yarn-lock-check-duplicates",
  "version": "1.0.2",
  "bin": {
    "yarn-lock-check-duplicates": "bin/check-duplicates"
  },
  "dependencies": {
    "args": "^5.0.1",
    "@yarnpkg/lockfile": "^1.1.0"
  }
}
